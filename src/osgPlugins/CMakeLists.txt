PROJECT(osgdb_cairo)

LINK_LIBRARIES(osgCairo)

ADD_LIBRARY(osgdb_cairo MODULE ReaderWriterCairo.cpp ${HEADER_FILES})

INSTALL(TARGETS osgdb_cairo LIBRARY DESTINATION ${OPENSCENEGRAPH_PLUGIN_DIR})

SET_TARGET_PROPERTIES(osgdb_cairo PROPERTIES PREFIX "")

